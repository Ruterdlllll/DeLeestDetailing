# De Leest Detailing

Website for De Leest Detailing — premium car & motorcycle detailing.

Static site (HTML/CSS/JS, no build step). Serve locally with:

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000
